# Cloudflare builds on Linux; this lockfile is generated with
# --os=linux --cpu=x64 --include=optional so every platform variant of
# sharp's native bindings is recorded. Without the optional entries npm ci
# reports the tree as out of sync and refuses to install.
include=optional
audit=false
fund=false
