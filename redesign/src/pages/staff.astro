---
import "@fontsource-variable/source-sans-3";
import "../styles/tokens.css";
import { categories } from "../data/categories.js";

const categoryOptions = categories.map((c) => ({
  slug: c.slug,
  name: c.name,
}));
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <title>Staff admin — ARK Hygiene Solutions</title>
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
  </head>

  <body>
    <!-- ===================== Login ===================== -->
    <div id="login" class="login">
      <form class="login__card" id="login-form">
        <div class="login__mark">ARK</div>
        <h1 class="login__title">Staff admin</h1>
        <label class="login__label" for="pw">Password</label>
        <input
          id="pw"
          type="password"
          autocomplete="current-password"
          required
          autofocus
        />
        <button type="submit" class="btn btn--primary" id="login-btn">
          Log in
        </button>
        <p class="login__error" id="login-error" hidden></p>
        <a href="/" class="login__back">&larr; Back to site</a>
      </form>
    </div>

    <!-- ===================== App ===================== -->
    <div id="app" hidden>
      <header class="bar">
        <div class="bar__left">
          <span class="bar__mark">ARK</span>
          <span class="bar__title">Catalogue</span>
          <span class="bar__count" id="count"></span>
        </div>
        <div class="bar__right">
          <span class="bar__status" id="status"></span>
          <button class="btn btn--ghost" id="add">+ Add product</button>
          <button class="btn btn--primary" id="save" disabled>
            Save changes
          </button>
          <button class="btn btn--ghost" id="logout">Log out</button>
        </div>
      </header>

      <div class="toolbar">
        <input
          type="search"
          id="search"
          placeholder="Search products…"
          autocomplete="off"
        />
        <select id="filter">
          <option value="">All categories</option>
          {
            categoryOptions.map((c) => (
              <option value={c.slug}>{c.name}</option>
            ))
          }
        </select>
      </div>

      <main class="list" id="list"></main>
    </div>

    {/* Categories come from the same source the site builds from, so the
        dropdown can never drift out of sync with the real category slugs. */}
    <script is:inline define:vars={{ categoryOptions }}>
      window.__CATEGORIES__ = categoryOptions;
    </script>
    <script is:inline src="/staff.js"></script>

    <style is:global>
      body {
        background: var(--surface);
      }

      /* ---------------- Login ---------------- */
      .login {
        min-height: 100vh;
        display: grid;
        place-items: center;
        padding: var(--space-m);
      }

      .login__card {
        display: grid;
        gap: var(--space-xs);
        width: 100%;
        max-width: 360px;
        padding: var(--space-xl);
        background: var(--paper);
        border: 1px solid var(--line);
        border-radius: var(--radius-lg);
        justify-items: stretch;
        text-align: left;
      }

      .login__mark {
        display: grid;
        place-items: center;
        width: 56px;
        height: 56px;
        border-radius: var(--radius);
        background: var(--oxblood-700);
        color: var(--gold-050);
        font-weight: 700;
        letter-spacing: 0.04em;
        margin-bottom: var(--space-2xs);
      }

      .login__title {
        font-size: var(--step-2);
        font-weight: 600;
        letter-spacing: -0.025em;
        margin-bottom: var(--space-s);
      }

      .login__label,
      .field label {
        font-size: var(--step--1);
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--muted);
      }

      .login__error {
        font-size: var(--step--1);
        color: var(--oxblood-600);
        margin: 0;
      }

      .login__back {
        font-size: var(--step--1);
        color: var(--muted);
        margin-top: var(--space-s);
      }

      .login__back:hover {
        color: var(--oxblood-700);
      }

      /* ---------------- Bar ---------------- */
      .bar {
        position: sticky;
        top: 0;
        z-index: 30;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: space-between;
        gap: var(--space-s);
        padding: var(--space-s) var(--space-m);
        background: var(--paper);
        border-bottom: 1px solid var(--line);
      }

      .bar__left,
      .bar__right {
        display: flex;
        align-items: center;
        gap: var(--space-s);
        flex-wrap: wrap;
      }

      .bar__mark {
        font-weight: 700;
        color: var(--oxblood-700);
        letter-spacing: 0.04em;
      }

      .bar__title {
        font-weight: 600;
      }

      .bar__count,
      .bar__status {
        font-size: var(--step--1);
        color: var(--muted);
      }

      .bar__status[data-tone="ok"] {
        color: #2f6f4f;
      }
      .bar__status[data-tone="err"] {
        color: var(--oxblood-600);
      }

      /* ---------------- Toolbar ---------------- */
      .toolbar {
        display: flex;
        gap: var(--space-s);
        padding: var(--space-m) var(--space-m) 0;
        max-width: 1100px;
        margin-inline: auto;
      }

      .toolbar input,
      .toolbar select {
        font-family: inherit;
        font-size: var(--step-0);
        padding: 0.6rem 0.8rem;
        border: 1px solid var(--line);
        border-radius: var(--radius-sm);
        background: var(--paper);
        color: var(--ink);
      }

      .toolbar input {
        flex: 1;
      }

      /* ---------------- List ---------------- */
      .list {
        max-width: 1100px;
        margin-inline: auto;
        padding: var(--space-m) var(--space-m) var(--space-3xl);
        display: grid;
        gap: var(--space-2xs);
      }

      .row {
        background: var(--paper);
        border: 1px solid var(--line);
        border-radius: var(--radius);
        overflow: hidden;
      }

      .row[data-dirty="true"] {
        border-color: var(--gold-500);
      }

      .row__head {
        display: grid;
        grid-template-columns: 48px 1fr auto auto;
        align-items: center;
        gap: var(--space-s);
        padding: var(--space-2xs) var(--space-s);
        cursor: pointer;
        width: 100%;
        background: none;
        border: 0;
        text-align: left;
        font: inherit;
        color: inherit;
      }

      .row__thumb {
        width: 44px;
        height: 44px;
        object-fit: contain;
        background: var(--white);
        border: 1px solid var(--line-soft);
        border-radius: var(--radius-sm);
        mix-blend-mode: multiply;
      }

      .row__name {
        font-weight: 600;
        letter-spacing: -0.01em;
      }

      .row__meta {
        font-size: var(--step--1);
        color: var(--muted);
      }

      .row__price {
        font-size: var(--step--1);
        color: var(--oxblood-700);
        font-variant-numeric: tabular-nums;
      }

      .row__chev {
        color: var(--muted);
        transition: transform 0.2s var(--ease);
      }

      .row[data-open="true"] .row__chev {
        transform: rotate(90deg);
      }

      .row__body {
        padding: var(--space-s) var(--space-s) var(--space-m);
        border-top: 1px solid var(--line-soft);
        display: grid;
        gap: var(--space-s);
      }

      .grid2 {
        display: grid;
        gap: var(--space-s);
      }

      @media (min-width: 700px) {
        .grid2 {
          grid-template-columns: 1fr 1fr;
        }
      }

      .field {
        display: grid;
        gap: var(--space-3xs);
      }

      .field input,
      .field select,
      .field textarea {
        font-family: inherit;
        font-size: var(--step-0);
        padding: 0.55rem 0.7rem;
        border: 1px solid var(--line);
        border-radius: var(--radius-sm);
        background: var(--white);
        color: var(--ink);
        width: 100%;
      }

      .field textarea {
        resize: vertical;
        line-height: 1.5;
      }

      .field input:focus,
      .field select:focus,
      .field textarea:focus {
        outline: none;
        border-color: var(--oxblood-700);
        box-shadow: 0 0 0 3px var(--oxblood-050);
      }

      .row__actions {
        display: flex;
        justify-content: flex-end;
      }

      .btn--ghost {
        background: transparent;
        border: 1px solid var(--line);
        color: var(--ink);
        font-size: var(--step--1);
        padding: 0.55em 0.9em;
      }

      .btn--ghost:hover {
        border-color: var(--ink);
      }

      .btn--danger {
        background: transparent;
        border: 1px solid var(--line);
        color: var(--oxblood-600);
        font-size: var(--step--1);
        padding: 0.55em 0.9em;
      }

      .btn--danger:hover {
        border-color: var(--oxblood-600);
        background: var(--oxblood-050);
      }

      .btn:disabled {
        opacity: 0.45;
        cursor: not-allowed;
      }

      .empty {
        padding: var(--space-2xl);
        text-align: center;
        color: var(--muted);
      }
    </style>
  </body>
</html>
